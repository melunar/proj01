/**
 * Created by haoyong on 2016/3/15.
 * 判断一个变量是不是数字
 * @param strNum 要判断是否为数字的参数
 */

/*
function isDigit(strNum) {
    var patrn = /^[0-9]*$/;
    if(patrn.exec(strNum) == null || strNum == "") {
        return true;
    }
    return false;
}*/
define([],function() {
    return false;/*{
        var patrn = /^[0-9]*$/;
        if(patrn.exec(strNum) == null || strNum == "") {
            return true;
        }
        return false;
    }*/
});